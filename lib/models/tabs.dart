import 'package:flutter/material.dart';

class MyTab {
  final Color color = const Color(0xff8B959E);
  final Widget? imageNotSelected;
  final Widget? imageSelected;
  MyTab({this.imageNotSelected, this.imageSelected});
}
